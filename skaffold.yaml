apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/dgozalo/p3:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/dgozalo/p3:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: p3
        chartPath: charts/p3
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/dgozalo/p3'
          image.tag: '{{.DIGEST_HEX}}'
